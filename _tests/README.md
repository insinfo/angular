This package is all the unit and component tests for `package:ngx_core`. They
are maintained here in order to avoid declaring circular dependencies between
`ngx_core` and `ngx_test`, as well as adding private testing infrastructure.
