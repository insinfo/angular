This package is all the unit and component tests for `package:ng_core`. They
are maintained here in order to avoid declaring circular dependencies between
`ng_core` and `ng_test`, as well as adding private testing infrastructure.
